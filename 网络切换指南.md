# 局域网切换指南

换路由器或换热点后，按以下步骤修改配置。

---

## 一、查看新 IP

在运行 EMQX 的电脑上打开 **CMD（命令提示符）**，输入：

```
ipconfig
```

找到 **无线局域网适配器 WLAN** 下的 **IPv4 地址**，例如：

```
192.168.1.105
```

记下这个 IP，后面都要用。

---

## 二、需要修改的文件

### 1. ESP32 — WiFi + EMQX IP

**文件**：`ESP32\ESP32\main\main.c`

```c
#define WIFI_SSID "M3307_2.4G"                         // ← 新的 WiFi 名称
#define WIFI_PASS "m3307dsgcxz"                         // ← 新的 WiFi 密码

#define EMQX_BROKER_URI "mqtt://192.168.123.59:1883"    // ← 新的 EMQX IP
```

> 改完重新编译烧录：`idf.py build` → `idf.py -p COMx flash monitor`

---

### 2. Qt 大屏 — EMQX IP

**文件**：`qt\StWHouseDash\mainwindow.cpp`

```cpp
m_mqttClient->connectToBroker("192.168.123.59", 1883);  // ← 新的 EMQX IP
```

> 改完重新编译运行 Qt 项目。

---

## 三、快速检查清单

| 项目 | 改什么 | 文件 |
|:---|:---|:---|
| WiFi 名称 | `WIFI_SSID` | `ESP32/ESP32/main/main.c` |
| WiFi 密码 | `WIFI_PASS` | `ESP32/ESP32/main/main.c` |
| EMQX IP (ESP32) | `EMQX_BROKER_URI` | `ESP32/ESP32/main/main.c` |
| EMQX IP (Qt) | `connectToBroker()` | `qt/StWHouseDash/mainwindow.cpp` |

---

## 四、验证

1. ESP32 上电 → 串口看日志：`WiFi 连上路由器啦! 分配到的 IP: xxx` → `成功连上本地 EMQX Edge 服务器！`
2. Qt 大屏打开 → 登录 → 数据正常刷新
3. Web 页面 `http://<EMQX_IP>:8083/mqtt` 能看到数据

---

*最后更新：2026-06-07*
