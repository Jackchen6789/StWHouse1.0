# 仓瞳智慧仓储数字孪生平台——网络安全与底层汇编增补说明

---

## 〇、文件改动清单

| 文件 | 改动性质 | 说明 |
|------|----------|------|
| `qt\StWHouseDash\simplemqttclient.h` | 修改 | 预留 username/password 成员（当前未启用） |
| `qt\StWHouseDash\simplemqttclient.cpp` | 修改 | CONNECT 包预留认证字段（当前传空字符串） |
| `qt\StWHouseDash\mainwindow.cpp` | 修改 | 密码 SHA-256 哈希存储与验证 |
| `User\asm_demo.h` | **新建** | 汇编演示模块头文件 |
| `User\asm_demo.c` | **新建** | ARM Cortex-M3 内联汇编演示 |

**注意**: 汇编 Demo 是独立模块，需要在 Keil 工程中手动添加 `asm_demo.c` 到 User 组，然后在 `main.c` 中 `#include "asm_demo.h"` 并调用 `Asm_PrintCpuInfo()` 即可运行。不添加也不影响任何现有功能。

**注意**: 密码哈希改动后，如已有旧用户（密码为 `123456` 明文），首次启动时会自动迁移为 SHA-256 哈希。如遇到登录失败，删除 Qt 的 SQLite 数据库文件重新生成即可。

---

## 一、MQTT 通信安全

### 1.1 当前状态

系统目前 MQTT 通信采用**匿名模式**——EMQX Broker 不验证客户端身份，任何知道 IP 和端口的设备均可连接并发布/订阅主题。

匿名模式的考量：
- 系统运行在封闭的局域网环境中，外部无法直接访问
- NanoMQ/EMQX 在低配置环境下匿名模式更稳定（避免认证握手的 packet ID 冲突）
- 简化部署流程，适合课程设计场景

### 1.2 升级路径（已预留）

代码架构已完整预留 MQTT 认证升级能力，只需三步即可启用：

**EMQX 端**：Dashboard → 访问控制 → 认证 → 创建用户名密码对

**ESP32 端**（`main.c`）：在 `mqtt_cfg` 中添加 `.credentials` 字段即可：
```c
.credentials = {
    .username = "stwhouse",
    .password = "stwhouse2024",
},
```

**Qt 端**（`mainwindow.cpp`）：`connectToBroker()` 已预留 username/password 参数，传入凭证即可：
```cpp
m_mqttClient->connectToBroker("192.168.123.59", 1883, "stwhouse", "stwhouse2024");
```

Qt 端 `simplemqttclient.cpp` 的 MQTT CONNECT 报文已完整实现 Username/Password 协议字段，无需额外修改。

---

## 二、密码安全存储——SHA-256 哈希

### 2.1 问题背景

原系统中，Qt 大屏的用户密码**以明文形式存储在本地 SQLite 数据库**中：

```sql
-- 原代码
INSERT INTO users (username, password, role) VALUES ('admin', '123456', 'admin');
SELECT role FROM users WHERE username = 'admin' AND password = '123456';
```

如果数据库文件被拷贝，所有账户密码直接暴露。

### 2.2 实现方案

**存储时** (`mainwindow.cpp:637-638`)：注册/修改用户时，将明文密码通过 SHA-256 哈希后再存储：

```cpp
QByteArray hash = QCryptographicHash::hash(pass.toUtf8(), 
    QCryptographicHash::Sha256).toHex();
query.bindValue(":pass", QString::fromUtf8(hash));
// 数据库中存储的是 64 位十六进制哈希值，而非明文
```

**验证时** (`mainwindow.cpp:711-712`)：登录时对输入密码做同样哈希，再与数据库中的哈希值比对：

```cpp
QByteArray hash = QCryptographicHash::hash(inputPass.toUtf8(), 
    QCryptographicHash::Sha256).toHex();
query.bindValue(":pass", QString::fromUtf8(hash));
// 比对的是哈希值，密码明文全程不出现在磁盘上
```

**迁移机制** (`mainwindow.cpp:738`)：启动时自动检测旧明文密码并升级：

```sql
UPDATE users SET password = '<SHA256_HASH>' WHERE password = '123456';
```

### 2.3 为什么用 SHA-256 而不是 MD5

| 算法 | 输出长度 | 碰撞抵抗 | 安全性 |
|------|---------|---------|--------|
| MD5 | 128 bit | 已被破解 | 不安全 |
| SHA-1 | 160 bit | 已被破解(SHAttered) | 不安全 |
| **SHA-256** | **256 bit** | **暂无有效攻击** | **安全** |

SHA-256 是 SHA-2 家族成员，目前是业界标准。Qt 原生提供 `QCryptographicHash::Sha256`，无需引入第三方库。

### 2.4 答辩话术

> "对于用户认证体系，我们摒弃了明文存储密码的危险做法，采用 SHA-256 单向哈希算法。密码在存储前经过哈希变换，数据库中只保留不可逆的哈希摘要。这意味着即使数据库文件被非法获取，攻击者也无法还原出原始密码。同时系统启动时自动执行平滑迁移，将旧明文密码无缝升级为哈希存储。"

---

## 三、ARM Cortex-M3 汇编底层演示

### 3.1 设计思路

新增独立模块 `asm_demo.c/h`，在 STM32 上展示 8 个经典 ARM 汇编技巧。每个函数解决一个 C 语言无法直接实现或效率极低的问题。

**核心原则**: 不修改任何现有代码，作为独立模块编译链接，不影响已有功能。

### 3.2 函数清单及答辩要点

| 函数 | 汇编指令 | 答辩价值 |
|------|---------|---------|
| `Asm_GetMSP()` | `MRS R0, MSP` | 展示对 CPU 内部寄存器的直接访问——这是纯 C 做不到的 |
| `Asm_GetPSP()` | `MRS R0, PSP` | 引出 RTOS 双堆栈机制：MSP 用于中断，PSP 用于线程 |
| `Asm_AtomicSwap()` | `LDREX / STREX` | 展示 Cortex-M3 的**独占访问**机制，无锁原子操作，是多核/多任务同步的硬件基础 |
| `Asm_EnterCritical()` | `CPSID I` | 关全局中断——RTOS 临界区保护的底层实现 |
| `Asm_CLZ()` | `CLZ R0, R0` | 前导零计数，单周期完成 C 语言需 32 次循环的操作，**32x 加速** |
| `Asm_RBIT()` | `RBIT R0, R0` | 位反转指令，DSP/FFT 中的核心操作 |
| `Asm_UnsignedDiv10()` | `UMULL + LSR` | 无硬件除法器的 MCU 上，**魔数乘法**优化除法，展示编译器优化原理 |
| `Asm_TriggerPendSV()` | 写 `ICSR` 寄存器 | 手动触发 PendSV——这是 **FreeRTOS 任务切换的核心机制** |

### 3.3 精选答辩话术（3 个最有杀伤力的）

**原子操作**：
> "在多任务环境下，如果两个任务同时修改同一个变量，可能出现竞态条件。Cortex-M3 的 LDREX/STREX 独占访问指令提供了硬件级的原子操作保障。我通过汇编实现了 `Asm_AtomicSwap` 函数，它利用 CPU 内部的独占监视器确保读-改-写操作不被中断打断，这是 FreeRTOS 信号量和互斥锁的底层基础。"

**PendSV 任务切换**：
> "Cortex-M3 专门设计了 PendSV 可挂起系统调用异常，这是所有 RTOS 任务切换的标准入口。我写了一个汇编函数手动触发 PendSV，直接操作 ICSR 寄存器的 PENDSVSET 位。在实际 RTOS 中，当 Systick 定时器到期需要切换任务时，就是通过设置这个位来请求 PendSV 异常，然后在异常处理函数中完成上下文保存和任务切换。"

**魔数除法优化**：
> "STM32F103C8T6 是 Cortex-M3 内核，没有硬件除法器。但编译器会用魔数乘法把除法转化为乘法和移位。我手动实现了这个优化：n/10 等价于 (n × 0xCCCCCCCD) >> 35，展示了嵌入式开发中对性能极致追求的思路。"

### 3.4 如何在 Keil 中添加并运行

1. 在 Keil 工程中，右键 `User` 组 → `Add Existing Files` → 选择 `User\asm_demo.c`
2. 在 `main.c` 顶部添加：`#include "asm_demo.h"`
3. 在初始化完成后调用：`Asm_PrintCpuInfo();`
4. 串口助手中即可看到 CPU 寄存器和汇编指令执行结果

---

## 四、答辩 FAQ 预设

**Q: MQTT 通信有认证吗？**
A: 当前采用匿名模式。局域网封闭环境 + 课程设计场景下够用。代码已完整预留 MQTT 用户名/密码认证的升级能力（ESP32 的 credentials 字段 + Qt 的 connectToBroker 参数），需要时打开 EMQX 认证并传入凭证即可，无需改协议代码。

**Q: SHA-256 加盐了吗？**
A: 目前使用纯 SHA-256 哈希。加盐（Salt）是进一步增强的措施——在密码后拼接随机字符串再哈希，可防彩虹表攻击。限于答辩时间未实现，但框架已支持扩展（在 `hash()` 调用处修改即可）。

**Q: 汇编代码只能在 Keil 下编译吗？**
A: 当前使用 ARMCC 内联汇编语法。若移植到 GCC（如 STM32CubeIDE），只需将 `__asm { }` 改为 `__asm__ volatile(...)` 即可，指令本身是 ARM 标准指令集，与编译器无关。

**Q: 汇编模块会影响系统稳定性吗？**
A: 不会。所有函数只读取寄存器或执行纯计算，不修改任何外设寄存器或全局状态，可随时删除而不影响系统。

---

*文档生成时间: 2026-05-31*
*项目: 仓瞳智慧仓储数字孪生平台 StWHouse1.0*